# 世界粘贴板 / World Clipboard

> 把现实世界，复制粘贴。
>
> Ctrl+C the real world. Ctrl+V anywhere.

**吉客松主题：没有聊天框的产品**

**核心理念：Promptless Interaction —— 动作本身就是 Prompt。**

**项目形态：原生微信小程序 + Python 视觉处理服务**

**提交版本：功能演示 Demo，2026 年 9 月 13 日**

## 1. 项目摘要

世界粘贴板是一款探索现实与数字内容之间交互方式的微信小程序。用户打开摄像头，将食指光标对准现实物体，通过拇指与食指捏合完成抓取，张开两指后将目标转换为数字内容。物体成为透明图片，颜色成为 HEX / RGB 色值，轮廓成为单色剪影。这些内容进入统一的 World Clipboard，再通过插件转换为拼豆图纸、贴纸、像素画、LEGO 平面拼搭模板或十字绣针位图。

项目用位置、动作和视觉上下文表达用户意图，探索一种没有聊天框的 Spatial GUI：用户先看见，再伸手选择，最后把现实中的内容带入创作。

## 2. 我们想解决什么问题

现实世界里有大量可以成为创作素材的内容：桌上的摆件、包装盒的形状、衣服上的颜色、一个有趣的剪影。然而，把这些内容带进数字工具，通常需要跨越多个操作步骤：拍照、选择图片、裁切、抠图、提取颜色，再切换软件制作模板。

这些步骤把用户的注意力从“我想使用这个东西”转移到“我要怎样操作工具”。在聊天式产品中，用户还需要把视觉意图转换成文字，描述目标、位置和处理要求。

世界粘贴板希望缩短这段距离：当用户已经看见目标时，直接用手的位置表达“这个”，用捏合表达“抓住”，用松开表达“复制”。摄像头画面既是输入来源，也是交互界面。

项目关注的核心问题是：**能否把电脑中熟悉的 Copy / Paste 心智模型，延伸到现实世界的视觉内容？**

## 3. 为什么契合“没有聊天框的产品”

在世界粘贴板中，用户意图由三个信息共同构成：

| 意图信息 | 用户如何表达 | 系统如何使用 |
| --- | --- | --- |
| 处理什么 | 将光标移到目标内部 | 形成视觉选点，结合可用候选框定位目标 |
| 提取什么 | 选择物体、颜色或轮廓模式 | 决定输出透明图片、色值或剪影 |
| 何时执行 | 捏合抓住，再张开两指 | 状态机确认选择与释放，触发复制流程 |

用户无需输入“请帮我抠出画面里的这个物体”。指向目标的动作已经提供了空间位置，捕捉模式提供了操作类型，捏合与释放提供了执行时机。

这就是项目所说的“动作本身就是 Prompt”：Prompt 从文字指令变成了可被系统解释的位置和动作。界面仍保留必要的模式选择、反馈与创作参数，让用户能够理解当前状态并控制结果。

## 4. 核心体验：SEE → PINCH → GRAB → COPY → CREATE

### 4.1 SEE：看见现实

打开小程序后直接进入后置摄像头画面。画面占据主要视觉空间，底部仅保留“物体 / 颜色 / 轮廓”三段选择器。

### 4.2 PINCH：指向目标并捏合

系统通过微信 VisionKit 获取手部关键点。食指尖驱动 Spatial Cursor，用户将光标放到目标内部，随后让同一只手的拇指与食指指尖相碰。

### 4.3 GRAB：锁定意图

确认捏合后，系统锁定选择位置及可用候选框，显示抓住反馈。保持捏合并移动手，可以演示 Drag；不拖动也可以直接松手复制。

### 4.4 COPY：松手后数字化

真实手势流程保持追踪直到确认两指张开，随后调用原生 Camera 拍摄高清照片，执行取色或分割，并显示 Copy 动画。成功后自动进入 Clipboard 页面。

点和候选框在 Grab 时锁定，高清照片在 Release 确认后获取。因此操作期间需要保持手机对准原目标，并尽量避免手部遮挡目标。触摸调试流程则在按下时拍照，松开后使用已保存的照片。

### 4.5 CREATE：选择创作方式

Clipboard 展示捕捉内容，用户选择 Paste Plugin，将同一个数字素材转换为不同创作结果。参数调整和图纸查看都在第二页内完成。

## 5. 两个页面，三种捕捉对象

### 5.1 现实捕捉页 / Camera

页面由实时摄像头画面、空间交互覆盖层和底部模式选择器构成。视觉以轻量半透明控件、小光标和目标反馈为主，尽量让 UI 退到现实画面之后。

支持三种模式：

| 模式 | 当前输出 | 可用于什么 |
| --- | --- | --- |
| 物体 | 原色透明 PNG、源照片、Mask 与目标 bbox | 贴纸、拼豆、像素画等创作素材 |
| 颜色 | 色块、HEX 与 RGB 数值 | 记录配色、提取现实中的视觉色彩 |
| 轮廓 | 单色透明剪影与外轮廓坐标 | 形状参考、图形设计及后续切割模板扩展 |

颜色在小程序端从照片选点附近读取最多 9×9 像素邻域，剔除亮度极端样本后求平均，减少单像素噪声影响。它不依赖分割后端，但仍会受相机曝光、白平衡和环境光影响。

轮廓复用分割 Mask，输出单色剪影，不保留物体的照片纹理。当前坐标数据描述最大外轮廓，PNG 中的透明孔洞由实际 Mask 决定；完整多环 SVG 编辑与导出属于后续方向。

### 5.2 世界粘贴板页 / World Clipboard

第二页以白色和浅灰背景、大留白、统一圆角承载刚刚抓取的内容。物体居中展示，颜色显示明确的色值，轮廓展示剪影。

下方是创作模板入口。进入插件后，页面切换为紧凑工作区，提供原图参考、参数、结果预览和材料统计。项目保持两个核心页面，没有复杂导航和账号系统。

这里的 Clipboard 是本项目内部的统一素材容器，当前保存于内存。宣传语中的“Ctrl+V anywhere”表达长期愿景；当前尚未实现跨设备粘贴、微信系统剪贴板写入或对任意外部软件的直接粘贴。

## 6. 五种 Paste Plugins

### 6.1 拼豆模板 / Perler

输入真实透明抠图，按 alpha 前景范围裁切并保持比例，降低分辨率、进行区域颜色采样和色卡量化，生成网格图案与材料清单。

当前支持：

- 32×32、48×48、64×64 网格；前端默认 64 格。
- 两种采样风格：“清晰色块”与“保留细节”。
- 8 / 16 / 24 色上限与 MARD 色卡映射。
- 实心图案预览、带坐标和色号的图纸、放大查看。
- 每个色号的用豆数量及总豆数统计。

透明区域不放豆，图案和材料统计来自同一网格数据。细节模式对有采样支持的暗线进行保留，但比一个网格更密的纹理和小字仍会简化。抠图中残留的背景也会进入图案，拼豆插件不会自动重新识别或修复分割结果。

### 6.2 贴纸 / Sticker

保留原物体颜色与细节，支持无白边、8px 或 16px 白边，提供透明 PNG 导出及六枚白底排版。适合把现实摆件、包装或随手发现的物体变成贴纸素材。

当前输出为位图图片，尚不提供矢量刀线或印刷设备校准。

### 6.3 像素画 / Pixel Art

将同一份 RGBA 素材转换为有限色像素图。支持网格大小、色数上限、图案与坐标色号图，以及最近邻放大的透明 PNG 导出，保持原始比例与透明区域。

### 6.4 LEGO 平面拼搭模板

把有限色网格中相邻同色区域合并为常用规格砖块，生成二维单层拼搭示意和按颜色、尺寸统计的砖块清单。

当前支持 1×1、1×2、1×3、1×4、2×2、2×3、2×4 等规格及旋转。砖块不会跨色覆盖，也不会填入透明孔洞；采用较大规格优先策略。

本版本是二维平面模板，不包含 3D 重建，不保证全局最少砖块数。颜色使用通用近似调色板，未对接官方零件编号、采购目录或实时库存。

### 6.5 十字绣模板 / Cross Stitch

将网格转换为一格一针的完整十字，生成绣制预览、坐标色号针位图、各颜色针数，以及基于 14CT 的全网格尺寸估算。透明区域不落针。

当前使用项目通用颜色编号，不提供 DMC 官方绣线匹配或真实耗线长度估算。

贴纸、像素画、LEGO 和十字绣工作区均提供结果放大与 PNG 保存操作。

## 7. 技术实现与架构

项目采用“手机负责交互，后端负责较重图像处理”的结构，减少在小程序中部署复杂视觉模型的成本。

```text
微信小程序
  后置摄像头预览 / VisionKit 手部关键点
                 ↓
  Spatial Cursor / Gesture Engine / State Machine
                 ↓
  Grab 锁定选点与候选框 → Release → 高清 Camera Photo
                 ↓
        ┌────────┴────────┐
        ↓                 ↓
  颜色：端侧邻域取色   物体 / 轮廓：上传视觉后端
        ↓                 ↓
        │          Point / Box → ROI → Mask Cleanup
        │                 ↓
        │          高清原图 RGB + Mask → PNG
        └────────┬────────┘
                 ↓
         统一 ClipboardItem
                 ↓
  拼豆 / 贴纸 / 像素画 / LEGO / 十字绣插件
```

### 7.1 小程序端

使用原生 TypeScript、WXML、WXSS，结合微信 Camera API、VisionKit、WebGL 相机渲染及 Canvas 2D 图像处理。核心职责包括手部追踪适配、光标显示、手势状态、选点锁定、拍照编排、动画、结果展示和用户主动触发的导出。

### 7.2 Python 后端

使用 FastAPI 提供图片上传和创作模板接口，使用 OpenCV 进行点 / 框引导的 GrabCut 分割、ROI 精修、连通域清理和轻量形态学处理。

最终分割使用低分辨率代理生成 Mask，再将 Mask 映射回高清原图 ROI。最终 PNG 的 RGB 来自高清照片，不从低清预览放大生成。当前 ROI 每侧扩边约 30%，精修输入长边上限 1024px。

当前分割实现是 OpenCV 管线，尚未接入 SAM、BiRefNet、RMBG 或 Alpha Matting；“Spatial AI Interface”是产品探索方向，不代表所有处理都由生成式模型完成。当前已经使用平台手部追踪能力和视觉算法解释动作、位置与图像上下文。

### 7.3 统一素材与插件边界

Clipboard 的基础类型保持为物体、颜色和轮廓，包含创建时间、源图片、预览、Mask、bbox、色值、轮廓和空间属性等字段。插件消费素材，生成自己的结果数据，不把拼豆格子或砖块布局写入基础素材类型。

这种边界让“现实内容如何被捕捉”和“内容如何用于创作”能够独立迭代。后续替换分割 Adapter 或新增创作插件，不需要重新设计整个页面流程。

主要接口：

| 接口 | 用途 |
| --- | --- |
| `GET /api/health` | 检查视觉服务状态 |
| `POST /api/segment` | 悬停预选及最终物体 / 轮廓分割 |
| `POST /api/perler` | 拼豆图案、图纸和材料统计 |
| `POST /api/templates/{kind}` | 贴纸、像素画、LEGO、十字绣转换 |

## 8. 手势交互为什么可以持续迭代

交互由独立状态机组织，核心过程为 Hover、Pinch Candidate、Grab、Drag 和 Release。主要稳定性措施包括：

- 用掌宽归一化两指尖距离，降低手离镜头远近变化的影响。
- 捏合与释放使用不同阈值，并结合独立观测数和时间进行确认。
- 使用 One Euro 滤波平衡慢速稳定性与快速跟手响应。
- 捏合前锁定选择历史，减少手指闭合时光标偏移导致选错目标。
- 对短暂丢失保留状态；长时间丢失取消抓取，不把丢手误当松手。
- 隔离旧相机会话回调、过期异步结果与重复释放事件。
- 真实手势确认 Release 后才交接原生相机，减少拍照暂停追踪造成的漏触发。

不支持真实追踪的环境可以通过按住、拖动和松开屏幕模拟交互。Debug 模式用于检查流程与坐标，不用固定动物图片伪装真实抓取成功。

## 9. 项目创新点与差异

### 9.1 用空间动作表达意图

目标位置由手的位置表达，执行时机由捏合和松开表达。用户可以在视觉场景中直接操作内容，减少把“这个东西”翻译成文字的过程。

### 9.2 把不同视觉信息统一为可用素材

物体、颜色和轮廓共同进入 Clipboard，使现实内容具有统一的数字入口。用户可以根据创作任务选择提取“对象本身”“颜色”或“形状”。

### 9.3 从素材捕捉连接到可执行创作

结果不仅是图片展示。拼豆数量、砖块清单和针位统计把数字素材进一步转换为可参考的实体创作步骤，连接现实观察、数字处理和手工制作。

### 9.4 同一份素材具有多个去向

Paste Plugins 让一次抓取支持多种创作形式，也为后续接入其他设计工具提供了模块边界。当前 Demo 已展示一对多转换，而跨应用、跨设备的 Paste 仍属于未来扩展。

项目借鉴了已有 AR Cut & Paste 等工作的启发，贡献在于将这些思路组合为微信小程序中的无聊天框交互、多类型素材容器和创作插件闭环，不主张基础算法或现实 Copy / Paste 概念的首创。

## 10. 应用场景

- **手工创作：** 抓取桌面摆件或有趣包装，生成拼豆、平面拼搭或十字绣参考图。
- **视觉设计：** 从现实物体提取贴纸、颜色或剪影，积累设计素材。
- **亲子与课堂活动：** 让学生选择身边对象，并观察照片、轮廓、像素和材料图纸之间的关系。
- **灵感记录：** 将日常看见的形状和色彩快速带入创作工作区。

这些是目标使用场景，尚未开展规模化用户研究，也不提供已验证的效率提升比例。

## 11. 当前完成情况与边界

| 能力 | 提交版本状态 |
| --- | --- |
| 原生微信小程序双页面 | 已实现 |
| 实时摄像头与 VisionKit 手部追踪 | 已接入，效果依设备及场景变化 |
| 光标、捏合、抓取、拖动、释放和 Copy 动画 | 已实现，最新松手拍照流程需继续真机复验 |
| 真实物体抠图与透明 PNG | 已实现 OpenCV 方案，复杂场景质量有限 |
| 端侧颜色提取 | 已实现，未做专业测色标定 |
| 单色剪影与外轮廓坐标 | 已实现，准确度依赖分割 Mask |
| 五种创作模板入口与生成 | 已实现，实体制作效果需结合材料验证 |
| Touch / Mouse 调试与失败恢复 | 已实现 |
| 跨设备 / 任意应用粘贴、素材历史库 | 未实现 |
| 学习式精细分割、半透明 Matting、3D 重建 | 未接入 |

当前更适合演示背景简单、主体完整、边缘清晰的不透明物体。透明瓶子、绒毛、反光材质、相接的同色背景以及手部大面积遮挡仍可能出现分割错误。小程序使用内存 Clipboard，重启后的素材历史持久化尚未实现。

## 12. 现场演示建议

建议准备一个与背景对比明显的包装盒或摆件，放在干净桌面上，保证主体完整进入画面。

1. 打开小程序，说明“无需输入描述，动作提供意图”。
2. 选择物体模式，食指光标指向物体内部，短暂停稳。
3. 捏合到出现“已抓住”，保持手机对准原目标，再在画面内张开两指；不必拖动。
4. 展示 Copy 反馈以及 Clipboard 中来自现场物体的透明图片。
5. 打开拼豆，展示原图、网格大小、图案、色号和材料数量之间的对应关系。
6. 返回模板，展示同一素材转换为贴纸或 LEGO 平面模板。
7. 如时间允许，再抓取一个明显色块或轮廓，展示三种素材类型。

现场应先确认相机权限、目标手机支持情况，以及手机到视觉服务的网络连接。真机访问电脑后端必须使用可达的局域网 IP 或已配置的 HTTPS 服务地址，不能使用手机自身的 `localhost / 127.0.0.1`。开发者工具的触摸模拟可作为交互流程演示备用，但应明确区分模拟手势与真实手部追踪。

## 13. 本地运行与验证

### 微信小程序

在仓库根目录运行 `npm install`，随后用微信开发者工具导入包含 `project.config.json` 的项目根目录。使用有开发权限的账号与有效 AppID，点击编译并生成新的真机预览。

### 视觉后端

Windows PowerShell 中启动方式：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

将小程序 `miniprogram/config.ts` 中的 `VISION_API_BASE_URL` 配置为手机能够访问的服务地址。当前服务面向本地或受信任局域网演示；正式对外部署仍需完成服务部署和访问配置。

### 验证记录

截至本介绍生成时，最近一次前端自动化检查共 **217 项测试通过**，小程序 TypeScript 检查与仓库 TypeScript 构建检查通过。覆盖包括手势确认、坐标映射、连续抓取、相机恢复、异步取消、请求超时以及插件工作区等。

自动化测试中的相机、平台会话和部分网络边界使用替身，因此测试通过不等同于目标手机的识别成功率或现场分割质量。此前已有真机体验反馈与多轮代码修复；最新版本仍应在比赛使用的设备和网络环境下完整复测。

## 14. 开源参考与项目实现

项目通过开源研究、许可范围内的代码改编与独立实现共同完成：

- **AR Cut & Paste：** 为现实对象复制到数字环境提供产品启发。
- **微信官方 miniprogram-demo：** VisionKit 手部会话与 YUV 相机渲染的参考和改编来源。
- **KineMouse：** 捏合滞回与状态机交互的研究参考。
- **Perler Beads Generator：** 拼豆区域采样、色差计算、候选色处理及 MARD 色卡的改编来源，并保留许可声明。
- **OpenCV / FastAPI：** 图像处理和视觉服务的基础工具。

项目实现了原生双页面、手势与拍照编排、三种素材分流、统一 Clipboard、插件工作区、透明贴纸排版、平面砖块覆盖及针位统计等整合功能。MediaPipe 是考察过的后续适配方向，当前真机追踪使用微信 VisionKit。

详细链接、改编范围与许可声明见仓库 [THIRD_PARTY_NOTICES.md](./THIRD_PARTY_NOTICES.md)。运行细节与开发记录见 [README.md](./README.md)。

## 15. 后续方向

优先在比赛设备上验证连续使用、松手复制、坐标一致性和现场网络，再逐步提升复杂场景的分割能力。

后续可沿三个方向扩展：一是接入同接口的更精细分割与边缘处理，提升复杂对象的素材质量；二是完善素材历史、轮廓导出和创作模板编辑；三是探索与桌面设计工具或其他应用的连接，让 Paste 从项目内部工作区延伸到更广泛的数字工具。

世界粘贴板希望证明的一件事是：当位置和动作足够明确时，用户可以直接用身体表达视觉意图，让身边的现实内容成为可继续创作的数字素材。
